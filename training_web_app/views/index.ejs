<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Text Labeling App</title>
  <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body { font-family: Arial, sans-serif; padding-top: 20px; }
    .container { max-width: 800px; }
    .text-entry { margin-bottom: 20px; }
    .label-input { margin-left: 10px; }
    .btn-label { margin-left: 10px; }
    .progress-bar { transition: width 0.5s; }
  </style>
</head>
<body>
  <div class="container">
    <h1 class="text-center">Text Labeling App</h1>
    
    <div class="progress mb-4">
      <div class="progress-bar" role="progressbar" style="width: <%= (index / total) * 100 %>%;" aria-valuenow="<%= index %>" aria-valuemin="0" aria-valuemax="<%= total %>"></div>
    </div>
    
    <form id="labelForm" method="POST" action="/label">
      <div class="card text-entry">
        <div class="card-body">
          <p class="card-text"><%= entry.text %></p>
          <input type="hidden" name="index" value="<%= index %>">
          <input type="text" name="label" value="<%= entry.label %>" placeholder="Enter label" class="form-control label-input">
          <button type="submit" class="btn btn-primary btn-label">Label</button>
        </div>
      </div>
    </form>
  </div>
  <script src="https://code.jquery.com/jquery